% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estpw_s5_multi.R
\name{ipwm_multi_build}
\alias{ipwm_multi_build}
\title{Build pseudo-weights for the multi-reference calibration method}
\usage{
ipwm_multi_build(
  sc,
  sp,
  vars,
  weight,
  sp_des,
  sp_order,
  control,
  verbose = FALSE,
  log_messages = NULL
)
}
\arguments{
\item{sc}{Data frame. The nonprobability sample.}

\item{sp}{List of data frames. Each element is one probability reference
sample (already NA-processed and p_formula-processed).}

\item{vars}{List of character vectors, one per reference sample, giving the
predictor variable names shared with `sc`.}

\item{weight}{Character vector, one per reference sample, giving the
survey-weight column name in each element of `sp`.}

\item{sp_des}{List of `survey.design2` or `svyrep.design` objects,
one per reference sample.}

\item{sp_order}{Character scalar, either `"size"` (sort reference samples
largest-first) or `"given"` (keep user order).}

\item{control}{A list created by `pw_solver_control()`.}

\item{verbose}{Logical. If `TRUE`, progress messages are printed.}

\item{log_messages}{Character vector of messages accumulated in earlier
steps, forwarded into the returned object.}
}
\value{
A list with components:
  \item{pseudo_weights}{Numeric vector of estimated pseudo-weights for `sc`.}
  \item{coefficients}{Named numeric vector of participation model coefficients.}
  \item{method}{Character scalar, always `"multi"`.}
  \item{solver_diagnostics}{List of solver convergence diagnostics.}
  \item{internal}{List of intermediate objects (block design matrices,
    reference weighted totals, block column indices, sandwich components,
    and sorted metadata) needed by the estimation stage.}
}
\description{
Build pseudo-weights for the multi-reference calibration method
}
\keyword{internal}
